# समासः Source Examples

The pinned tests contain **167** recognized compound assertions: **132 active** and **35 ignored**. The extractor can reconstruct 152 directly from literal members; 15 use locally constructed inputs and remain explicit exclusions in this bounded pass.
